const COLORS_ENABLED_TYPES = {
  ACTIVE: 1,
  INACTIVE: 0,
};

const LOG_LEVEL_TYPES = {
  INFO: 'info',
  WARN: 'warn',
  ERROR: 'error',
};

const constants = {
  COLORS_TYPES: COLORS_ENABLED_TYPES,
  LEVEL_TYPES: LOG_LEVEL_TYPES,
};

module.exports = constants;
